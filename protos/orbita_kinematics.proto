syntax = "proto3";

package reachy.sdk.kinematics;

import "kinematics.proto";

message Quaternion {
    double w = 1;
    double x = 2;
    double y = 3;
    double z = 4;
}

message OrbitaTarget {
    Quaternion q = 1;
}

message Point {
    double x = 1;
    double y = 2;
    double z = 3;
}

service OrbitaKinematic {
    rpc ComputeOrbitaIK(OrbitaTarget) returns (JointsPosition);
    rpc GetQuaternionTransform(Point) returns (OrbitaTarget);
}
